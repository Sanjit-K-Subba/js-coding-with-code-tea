const accountID = 12345;
let accountEmail = "sanjeevlimbu@gmail.com";
var accountPassword = "54321";
accountCity = "Kathmandu"; //this way also can create var, but never to use//
let accountState; //variable is not defined here//

// accountID = 56789;, Constant is not changble.//
//const is always use (const)//
/* to set the variables we use (let) not (var) i mean can use but better we use let */
// prefer not to use var//
//becuz issue in block scope and functional scope//

console.log(accountID);
console.table([accountID, accountEmail, accountPassword, accountCity, accountState]);